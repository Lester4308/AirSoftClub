# Implementation 008 — playable development client

Unity management client and loopback ASP.NET intents implement entry/reconnect, starter choice, hub, roster, training/heal, recruitment/refresh, shop/four slots, shared BB, opponents/confirmation, immutable event playback and history. Every ready fighter participates automatically. UI uses independent IMGUI placeholders, not final art. Server validates all writes; no trusted result/reward input. Network retry retains the same operation key and payload; reconnect reloads durable state.

Actual HTTP smoke PASS: starter10, hire/buy/equip/train/heal/refill/attack/settlement/reconnect, each command submitted twice without duplication. Actual Mono executable UI coroutine passed the same backend loop. Mono Windows build PASS. Graphics verification remains pending: hidden D3D12 screenshot capture failed; hidden D3D11 generated black frames, explicitly not accepted as UI visual evidence. A visible-window verification was requested because the environment requires explicit permission for a visible launch. tools/verify-unity.ps1 now waits for the actual Unity process instead of waiting indefinitely for its unrelated licensing child processes.

Launch backend: tools/run-development.ps1. Launch Artifacts/Mono/AirsoftClubIntegration.exe; Connect with generated dev-account. This is loopback-only development auth, never production Steam identity. Production routes fail closed. tools/verify-http.ps1 records Artifacts/http-smoke.json. Current Revenge button remains disabled pending counterparty product policy. Retention and Steam adapters follow009–010; native/current runtime matrix and visual verification remain011 gates.
