# Відновлення після очищення — 09.09.2026

## Завдання та межі

За дорученням користувача відновлено локальний доступ до БД після очищення, виправлено документацію та збережено матеріали діагностики. Базовий коміт: 6b54be6. Gameplay/UI-дефекти цим виправленням не усуваються.

## Виконані зміни

1. Чинний пароль отримано з конфігурації наявного контейнера airsoft-club-development-db-1. Значення не виводилося в консоль, звіт або Git.
2. Пароль збережено в `.local/local-db-password.txt`, поза Artifacts. Додано `.local/` до .gitignore. Старий файл у Artifacts не створювали повторно: канонічний шлях перенесено, щоб наступне очищення збірок не знищило налаштування.
3. tools/start-db.ps1: читає постійний файл; за його відсутності переносить старий файл з Artifacts або відновлює пароль із одного наявного Compose DB-контейнера. Якщо наявний volume не має доступного джерела пароля, зупиняється з поясненням. Новий пароль генерується лише для перевірено нового оточення без контейнера/volume. Помилка Docker не запускає генерацію пароля.
4. README: один штатний PowerShell-сценарій запуску, правильна версія PostgreSQL 18.6, посилання на діагностику, чесний статус UI і PlayMode. Прибрано непрацюючі PowerShell-команди export та заяву про повністю функціональний UI.
5. CLEANUP_REPORT: виправлено висновки про golden-ресурси, PlayMode, ризики Artifacts, кількість PNG та статус незакомічених матеріалів. Первинний звіт доступний в історії Git.
6. Збережено DiagnosticAuditTests.cs з .meta, повний діагностичний звіт і його докази. Вони залишаються потрібною регресійною перевіркою, навіть поки тест падає.

## Фактична перевірка після змін

- `tools/start-db.ps1 -Migrate`: exit 0, контейнер healthy, доступ із відновленим паролем і міграції успішні.
- Старий локальний backend та його dotnet-launcher зупинено; запущено штатний `tools/run-development.ps1` у фоновому PowerShell. Базу/volume та клієнт не зупиняли й не видаляли.
- Новий сервер: `/ready` HTTP 200.
- `tools/verify-http.ps1`: PASS — окремий тестовий акаунт; старт, найм, покупка, екіпірування, тренування, лікування, поповнення, бій, settlement, повторний вхід. Повторна відправка кожної команди перевірена.
- `git check-ignore .local/local-db-password.txt`: файл ігнорується.
- `git diff --check`: PASS.

Журнали поточного запуску: Artifacts/recovery-server.stdout.log та recovery-server.stderr.log. Вони локальні, не включені в Git. Секрети не включено в діагностичні докази.

## Що НЕ заявляється як виправлене

- CLOSE/EQUIP у модальному інвентарі досі блокуються.
- Settings досі відкриває Club; інші UI-дефекти описані в DIAGNOSTIC_AUDIT_2026-09-09.md.
- Остання перевірка PlayMode при аудиті очищення: 7 PASS / 1 FAIL, golden smoke PASS. Це відомий тестовий failure, не timeout і не відсутні ресурси.
- Нові Mono/IL2CPP-збірки в цій роботі не створювалися, бо runtime-код не змінювався.
- Тестові акаунти/діагностична база не очищалися; дані користувача не видалялися.

## Подальше обслуговування

Запускати tools/run-development.ps1 згідно з README. `.local/` і Docker volume — постійні локальні дані, не кеш. Для нового комп'ютера без локальної БД буде створено власний пароль; секрет не переноситься через Git. Не повертати старий README або твердження про повністю робочий UI до перевірки регресій.
