# Implementation 004 — club lifecycle

Pure server domain now manages one-time free choice, seven offers, refresh1h OR10 completed settlements, paid refresh reset, max16, archive/refund30% original price and returned gear. Last active defender cannot be dismissed. Recovery uses raw HP and millisecond remainder, old MaxHP before training; one percent/minute and soft healing are prototype policies. All ready fighters automatically enter offense, all active full-HP fighters enter immutable defense with captured capacity.

Catalog includes six weapon families, Base=MK1/MK2soft/MK3Credits, four slots, light/medium/heavy armor. BB uses five finite per-tier capacities, one selected tier, explicit500 refill SKU capped without overflow; flat SKU cost even at partial capacity is a prototype choice. Emergency500 Basic below15% and Money<50 every3h. Initial500 Basic/1000 Money need economy simulation at011. Catalog prices/stats are hypotheses. Offers remain valid until regeneration; refresh invalidates the previous version. Public application commands will wrap these mutation methods in durable idempotent transactions in005–006; do not call repeated mutation methods directly as an HTTP handler.

Verification: dotnet run --project tests/Airsoft.Club.Tests -c Release:14 scenarios passed, including003's6. Covers sixteen cap, permanent starter, refresh boundaries/dedup, dismissal/gear, fractional and offline recovery, MaxHP upgrades, ready/weaponless/defense, emergency threshold/cooldown and pending lock. Build0warnings/errors. Combat code and golden unchanged. Corrected an extra trailing blank line from003 (no behavior change).
