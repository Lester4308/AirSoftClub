# Implementation 009 — Steam adapter foundation

Unity wrapper Steamworks.NET2025.164.1, release commit c21a8f0e31c56ae8707130967faf491f7dd7c0d8, pinned UPM tag plus resolved package lock. [Official wrapper release/install](https://github.com/rlabrecque/Steamworks.NET/releases/tag/2025.164.1). Explicit adapter requests GetAuthTicketForWebApi with service identity, polls callbacks, cancels tickets, enumerates discovery friends and rejects missing/mismatched configured AppID. It never initializes by default; package-generated sample steam_appid480 is ignored and not shipped as our identity.

Backend uses official [AuthenticateUserTicket](https://partner.steamgames.com/doc/webapi/ISteamUserAuth) and [CheckAppOwnership/GetFriendList](https://partner.steamgames.com/doc/webapi/ISteamUser). Publisher key is server environment only, dedicated nonlogging HTTPS client. Ticket is hex-validated; verified identity determines account, not client SteamID. Persistent hashed sessions and unique exchanged-ticket hash reject replay; expiry1h. Friend intent checks server API relationship. Development login requires Development+explicit flag+loopback; production cannot use development sessions. Borrowed ownership is conservatively rejected pending sharing policy; no live ownership claim is made.

Fresh verification: actual PostgreSQL13/13 scenarios (10 previous+3 session/Steam fixture scenarios). Explicit fixtures cover invalid/expired response, malformed ticket, wrong owner, nonownership, replay across new service instance, bad token and expiry. Unity EditMode9/9 after package and adapter compilation, zero C# errors/warnings.

BLOCKED EXTERNAL: no STEAM_APP_ID or STEAM_PUBLISHER_KEY configured, no provided sandbox app/entitled accounts. Actual Steam ticket/social/ownership roundtrip NOT RUN. Adapter exists; current playable UI continues using clearly marked local development accounts. Live Steam sign-in rollout and complete production matchmaking discovery remain unverified; do not treat fixture tests as live integration. No payments or publishing performed.
